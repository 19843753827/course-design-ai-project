### 制造智能技术课程设计 {#制造智能技术课程设计 .unnumbered}

基于轻量化卷积神经网络的

工业零部件表面缺陷智能检测系统

##### 课程名称 制造智能技术 {#课程名称-制造智能技术 .unnumbered}

##### 设计题目 工业零部件表面缺陷智能检测系统技术架构 B/S 架构 ▲ Flask ▲ PyTorch ▲ SQLite {#设计题目-工业零部件表面缺陷智能检测系统技术架构-bs-架构-flask-pytorch-sqlite .unnumbered}

##### 开发方式 Vibe Coding（AI 辅助人机协同开发）完成日期 2026 年 9 月 {#开发方式-vibe-codingai-辅助人机协同开发完成日期-2026-年-9-月 .unnumbered}

# 摘 要 {#摘-要 .unnumbered}

###### 本项目面向机械制造、汽车零部件、五金冲压等离散制造场景中的产品表面质量检测环节，开发了一套基于机器视觉与轻量化卷积神经网络（CNN）的智能缺陷检测系统。系统采用 B/S 架构，自上而下分为前端 UI 层、后端服务层、算法模块层和数据存储层，支持工业工件图片上传、缺陷自动识别分类、检测结果可视化展示与历史数据追溯。 {#本项目面向机械制造汽车零部件五金冲压等离散制造场景中的产品表面质量检测环节开发了一套基于机器视觉与轻量化卷积神经网络cnn的智能缺陷检测系统系统采用-bs-架构自上而下分为前端-ui-层后端服务层算法模块层和数据存储层支持工业工件图片上传缺陷自动识别分类检测结果可视化展示与历史数据追溯 .unnumbered}

###### 系统运用《制造智能技术》课程的三个核心技术方向：工业机器视觉与图像处理技术（图像灰度化、高斯滤波降噪、缺陷特征提取）、工业机器学习与智能分类识别技术（基于 CNN 的缺陷类型自动识别）、工业质量数据管理与追溯技术（检测数据存储、历史记录查询、质量统计分析）。 {#系统运用制造智能技术课程的三个核心技术方向工业机器视觉与图像处理技术图像灰度化高斯滤波降噪缺陷特征提取工业机器学习与智能分类识别技术基于-cnn-的缺陷类型自动识别工业质量数据管理与追溯技术检测数据存储历史记录查询质量统计分析 .unnumbered}

###### 算法核心采用 3 个卷积块 + 2 个全连接层的轻量化 CNN 网络，总参数量约 10.2 {#算法核心采用-3-个卷积块-2-个全连接层的轻量化-cnn-网络总参数量约-10.2 .unnumbered}

###### 万，在测试集上达到 100% 分类准确率，单张图片平均推理时间约 12 ms（CPU 环境）。系统通过自动化测试覆盖图片上传、预处理、模型推理、结果返回、数据库写入、历史查询全流程，达到可现场演示状态。本项目全程采用 Vibe Coding 方法，使用 AI {#万在测试集上达到-100-分类准确率单张图片平均推理时间约-12-mscpu-环境系统通过自动化测试覆盖图片上传预处理模型推理结果返回数据库写入历史查询全流程达到可现场演示状态本项目全程采用-vibe-coding-方法使用-ai .unnumbered}

###### 辅助开发并如实披露使用过程。 {#辅助开发并如实披露使用过程 .unnumbered}

###### 关键词：表面缺陷检测；轻量化卷积神经网络；机器视觉；B/S 架构；Vibe Coding；工业质量控制 {#关键词表面缺陷检测轻量化卷积神经网络机器视觉bs-架构vibe-coding工业质量控制 .unnumbered}

######  {#section .unnumbered}

> 目 录

[摘 要 I](\l)

[第一章 背景调研与研究意义 1](\l)

1.  
2.  
3.  

[工业背景与问题提出 1国内外研究现状 1研究意义与应用价值 2第二章 系统方案设计 3](\l)

1.  
2.  
3.  
4.  

[系统总体架构设计 3技术方向映射 3功能模块设计 4技术栈选型 5第三章 数据资源构建 6](\l)

1.  
2.  
3.  

[数据集来源与说明 6数据预处理与划分 6数据加载模块设计 7第四章 系统实现 9](\l)

1.  
2.  
3.  
4.  
5.  
6.  

[前端 UI 层实现 9后端服务层实现 10数据存储层实现 12算法模块层------CNN 模型设计 13算法模块层------模型训练 15算法模块层------推理封装 17第五章 测试与集成调试 19](\l)

1.  
2.  
3.  

[模型评估结果 19系统集成测试 20调试记录与问题解决 21第六章 AI 使用披露 22](\l)

1.  
2.  
3.  

[使用工具与环节 22关键 Prompt 策略 22AI 出错案例及纠正过程 23第七章 总结与展望 24](\l)

[参考文献 25](\l)

# 第一章 背景调研与研究意义 {#第一章-背景调研与研究意义 .unnumbered}

## 工业背景与问题提出

###### 在现代制造业中，产品表面质量直接影响产品的性能、可靠性和使用寿命。机械制造、汽车零部件、五金冲压等离散制造场景中，工件表面可能出现裂纹、夹杂、斑块、麻点、氧化铁皮、划痕等多种缺陷。传统的表面质量检测主要依赖人工目视检测，存在以下突出问题 {#在现代制造业中产品表面质量直接影响产品的性能可靠性和使用寿命机械制造汽车零部件五金冲压等离散制造场景中工件表面可能出现裂纹夹杂斑块麻点氧化铁皮划痕等多种缺陷传统的表面质量检测主要依赖人工目视检测存在以下突出问题 .unnumbered}

> ：

###### 检测效率低：人工检测速度慢，难以匹配高速生产线的节拍；

###### 漏检率高：长时间工作后人眼疲劳，细微缺陷容易漏检；

###### 标准不一致：不同检测人员对缺陷的判断标准存在主观差异；

###### 数据难以追溯：人工检测结果多为纸质记录，难以进行统计分析和质量追溯。

###### 随着机器视觉和深度学习技术的发展，基于卷积神经网络的自动缺陷检测成为工业质量控制的重要方向。本项目旨在开发一套轻量化、易部署的智能缺陷检测系统，辅助人工质检 {#随着机器视觉和深度学习技术的发展基于卷积神经网络的自动缺陷检测成为工业质量控制的重要方向本项目旨在开发一套轻量化易部署的智能缺陷检测系统辅助人工质检 .unnumbered}

###### ，提升检测效率与一致性。 {#提升检测效率与一致性 .unnumbered}

## 国内外研究现状

###### 工业表面缺陷检测经历了从传统图像处理到深度学习的发展历程。传统方法主要依靠人工设计特征（如边缘检测、纹理分析、阈值分割），对光照变化和缺陷形态多样性的鲁棒性较差。近年来，卷积神经网络（CNN）凭借强大的特征自动提取能力，在缺陷检测领域取得了显著进展。 {#工业表面缺陷检测经历了从传统图像处理到深度学习的发展历程传统方法主要依靠人工设计特征如边缘检测纹理分析阈值分割对光照变化和缺陷形态多样性的鲁棒性较差近年来卷积神经网络cnn凭借强大的特征自动提取能力在缺陷检测领域取得了显著进展 .unnumbered}

###### 在数据集方面，东北大学宋克臣团队发布的 NEU-CLS {#在数据集方面东北大学宋克臣团队发布的-neu-cls .unnumbered}

###### 热轧钢表面缺陷数据集是该领域的经典公开数据集，包含 6 类典型缺陷、每类 300 张共 1800张灰度图像，被广泛用于算法验证。在模型方面，从早期的 LeNet、AlexNet 到 ResNet、MobileNet 等轻量化网络，研究者在保证准确率的同时不断降低模型参数量和推理延迟，以适应工业现场的实时性需求。 {#热轧钢表面缺陷数据集是该领域的经典公开数据集包含-6-类典型缺陷每类-300-张共-1800张灰度图像被广泛用于算法验证在模型方面从早期的-lenetalexnet-到-resnetmobilenet-等轻量化网络研究者在保证准确率的同时不断降低模型参数量和推理延迟以适应工业现场的实时性需求 .unnumbered}

## 研究意义与应用价值

###### 本项目的研究意义体现在以下三个层面： {#本项目的研究意义体现在以下三个层面 .unnumbered}

######  {#section-1 .unnumbered}

###### 工程应用价值：系统采用 B/S 架构，无需在产线部署重型硬件，通过浏览器即可完成图片上传和缺陷检测，适合中小企业快速落地。轻量化 CNN 模型参数量仅约 10

###### 万，可在普通 CPU 上实现实时推理，降低了部署门槛。 {#万可在普通-cpu-上实现实时推理降低了部署门槛 .unnumbered}

###### 技术整合价值：项目整合了机器视觉、深度学习、Web 开发、数据库技术等多个领域，完整实现了从数据采集、模型训练到系统部署的全流程，是对《制造智能技术》课程所学知识的综合应用。

###### 方法探索价值：项目全程采用 Vibe Coding（AI 辅助编程）方法，探索了 AI 工具在工业软件开发中的有效使用模式，包括提示词工程、代码审查、调试纠错等，为同类项目提供参考。

######  {#section-2 .unnumbered}

# 第二章 系统方案设计 {#第二章-系统方案设计 .unnumbered}

## 系统总体架构设计

###### 系统采用完整的 B/S（Browser/Server，浏览器/服务器）架构，自上而下分为四层，各层之间通过标准协议通信，实现前后端解耦和算法模块独立封装： {#系统采用完整的-bsbrowserserver浏览器服务器架构自上而下分为四层各层之间通过标准协议通信实现前后端解耦和算法模块独立封装 .unnumbered}

###### 前端 UI 层：基于 HTML5 + CSS3 + JavaScript + Bootstrap，负责图片上传、检测结果展示、历史记录查询、统计图表展示；

###### 后端服务层：基于 Python Flask

###### 框架，负责路由管理、业务逻辑调度、文件处理、REST API 接口管理； {#框架负责路由管理业务逻辑调度文件处理rest-api-接口管理 .unnumbered}

###### 算法模块层：基于 OpenCV + PyTorch，负责图像预处理（灰度化、降噪、归一化）、CNN 模型推理分类、结果后处理；

###### 数据存储层：基于 SQLite 轻量级嵌入式数据库，存储检测记录与图片路径。系统架构图如下：

  -------------------------------------- ----------------------------------------------
  前端 UI 层 (HTML/CSS/JS + Bootstrap)   图片上传 \| 结果展示 \| 历史查询 \| 统计图表

  ↓ HTTP / REST API                      

  后端服务层 (Python Flask)              路由管理 \| 业务逻辑 \| 文件处理 \| 接口调度

  ↓ 函数调用                             

  算法模块层 (OpenCV + PyTorch)          图像预处理 \| CNN 模型推理 \| 结果后处理

  ↓ SQL                                  

  数据存储层 (SQLite)                    检测记录表 \| 图片路径 \| 统计数据
  -------------------------------------- ----------------------------------------------

####### 图 2-1 系统四层架构图 {#图-2-1-系统四层架构图 .unnumbered}

## 技术方向映射

###### 根据任务书要求，系统须覆盖《制造智能技术》课程至少 3 个核心技术方向，且每个方向须有实际作用，仅装饰性引入不计入覆盖。本项目的技术方向映射如下： {#根据任务书要求系统须覆盖制造智能技术课程至少-3-个核心技术方向且每个方向须有实际作用仅装饰性引入不计入覆盖本项目的技术方向映射如下 .unnumbered}

######  {#section-3 .unnumbered}

####### 表 2-1 课程技术方向映射表 {#表-2-1-课程技术方向映射表 .unnumbered}

## 功能模块设计

###### 系统包含以下核心功能模块： {#系统包含以下核心功能模块 .unnumbered}

###### 缺陷检测模块：支持 JPG/PNG/BMP 格式图片上传，自动进行图像预处理和 CNN推理，展示预测类别、置信度百分比、各类别概率条形图；

###### 历史记录模块：自动保存每次检测记录（图片路径、预测结果、置信度、检测时间）

###### ，支持按时间范围、缺陷类别筛选查询，分页浏览； {#支持按时间范围缺陷类别筛选查询分页浏览 .unnumbered}

###### 统计分析模块：对历史检测数据进行统计，展示各类缺陷分布饼图、检测趋势折线图

> ；

###### 系统管理模块：模型权重加载、检测阈值配置、数据库初始化。

## 技术栈选型

+------------+--------------------------------------+-------------------------------------+
| > 层级     | > 技术                               | > 版本/说明                         |
+------------+--------------------------------------+-------------------------------------+
| > 前端 HT  | > ML5 + CSS3 + JavaScript + Bootstra | p 响应式页面，Chart.js 绘制统计图表 |
+------------+--------------------------------------+-------------------------------------+
| > 后端     | > Python Flask                       | > 轻量级 Web 框架，RESTful API      |
+------------+--------------------------------------+-------------------------------------+
| > 算法框架 | > OpenCV + PyTorch                   | > 图像处理 + 深度学习推理           |
+------------+--------------------------------------+-------------------------------------+
| > 数据库   | > SQLite                             | 轻量级嵌入式数据库，无需额外安装    |
+------------+--------------------------------------+-------------------------------------+
| > 开发方式 | > Vibe Coding                        | 豆包 AI + 通义灵码，人机协同开发    |
+------------+--------------------------------------+-------------------------------------+
| > 版本控制 | > Git + GitHub                       | > 代码托管与版本管理                |
+------------+--------------------------------------+-------------------------------------+

####### 表 2-2 系统技术栈 {#表-2-2-系统技术栈 .unnumbered}

#######  {#section-4 .unnumbered}

# 第三章 数据资源构建 {#第三章-数据资源构建 .unnumbered}

## 数据集来源与说明

###### 本项目以 NEU-CLS 东北大学热轧钢表面缺陷数据集为参考基准。该数据集是工业缺陷检测领域的经典公开数据集，包含 6 类典型表面缺陷，每类 300 张共 1800 张 200×200 {#本项目以-neu-cls-东北大学热轧钢表面缺陷数据集为参考基准该数据集是工业缺陷检测领域的经典公开数据集包含-6-类典型表面缺陷每类-300-张共-1800-张-200200 .unnumbered}

###### 像素灰度图。 {#像素灰度图 .unnumbered}

+------------+-------------------+------------+--------------------------------------+
| > 类别编号 | > 英文名称        | > 中文名称 | > 缺陷特征说明                       |
+------------+-------------------+------------+--------------------------------------+
| > 0        | > Crazing         | > 裂纹     | > 表面网状细微裂纹，呈发散状分布     |
+------------+-------------------+------------+--------------------------------------+
| > 1        | > Inclusion       | > 夹杂     | > 非金属夹杂物，表现为不规则深色斑点 |
+------------+-------------------+------------+--------------------------------------+
| > 2        | > Patches         | > 斑块     | > 不规则深色斑块，面积较大           |
+------------+-------------------+------------+--------------------------------------+
| > 3        | > Pitted Surface  | > 麻点     | > 点状凹坑密集分布                   |
+------------+-------------------+------------+--------------------------------------+
| > 4        | > Rolled-in Scale | > 氧化铁皮 | > 轧制过程中氧化皮压入，呈条纹状     |
+------------+-------------------+------------+--------------------------------------+
| > 5        | > Scratches       | > 划痕     | > 线性划伤痕迹，方向不一             |
+------------+-------------------+------------+--------------------------------------+

####### 表 3-1 缺陷类别说明 {#表-3-1-缺陷类别说明 .unnumbered}

## 数据预处理与划分

###### 数据预处理是保证模型训练效果的关键环节。本项目的预处理流程包括以下步骤： {#数据预处理是保证模型训练效果的关键环节本项目的预处理流程包括以下步骤 .unnumbered}

###### 数据集扫描与索引生成：遍历 6 个类别文件夹，生成包含图片路径和标签的完整索引 CSV 文件（dataset_index.csv）；

###### 分层抽样划分：按 70% / 15% / 15%

###### 的比例划分为训练集、验证集、测试集，采用分层抽样保证每个子集中各类别比例一致； {#的比例划分为训练集验证集测试集采用分层抽样保证每个子集中各类别比例一致 .unnumbered}

###### 编码格式处理：CSV 文件使用 UTF-8-sig 编码，确保 Excel 打开中文不乱码；

###### 随机种子固定：固定随机种子为 42，保证数据划分结果可复现。数据划分结果如下：

+----------+--------+------------------------+------------------------------+
| > 子集   | > 比例 | > 样本数（1800张基准） | > 用途                       |
+----------+--------+------------------------+------------------------------+

+----------+--------+-------------------+------------------------------+
| > 训练集 | > 70%  | > 1260 张         | > 模型参数学习               |
+----------+--------+-------------------+------------------------------+
| > 验证集 | > 15%  | > 270 张          | > 超参数调优、早停判断       |
+----------+--------+-------------------+------------------------------+
| > 测试集 | > 15%  | > 270 张          | > 最终模型性能评估           |
+----------+--------+-------------------+------------------------------+
| > 合计   | > 100% | > 1800 张         | > ---                        |
+----------+--------+-------------------+------------------------------+

####### 表 3-2 数据集划分 {#表-3-2-数据集划分 .unnumbered}

###### 预处理核心代码（preprocess.py 关键逻辑）： {#预处理核心代码preprocess.py-关键逻辑 .unnumbered}

####### 代码 3-1 数据分层划分核心代码 {#代码-3-1-数据分层划分核心代码 .unnumbered}

> \# 分层抽样划分：保证每个类别在各子集中比例一致 from sklearn.model_selection import train_test_split
>
> \# 先分出训练集（70%），剩余 30% 再均分验证集和测试集 train_df, temp_df = train_test_split(
>
> df, test_size=0.3, random_state=42, stratify=df\[\'label\'\]
>
> )
>
> val_df, test_df = train_test_split(
>
> temp_df, test_size=0.5, random_state=42, stratify=temp_df\[\'label\'\]
>
> )
>
> \# UTF-8-sig 编码：Excel 打开中文不乱码 train_df.to_csv(\'data/processed/train_index.csv\', index=False, encoding=\'utf-8-sig\')

## 数据加载模块设计

###### 数据加载模块（dataset.py）是连接数据文件和 PyTorch 训练流程的桥梁，核心是实现 PyTorch 的 Dataset 接口。以下是关键代码及逐行解释： {#数据加载模块dataset.py是连接数据文件和-pytorch-训练流程的桥梁核心是实现-pytorch-的-dataset-接口以下是关键代码及逐行解释 .unnumbered}

####### 代码 3-2 DefectDataset 数据加载类（dataset.py） {#代码-3-2-defectdataset-数据加载类dataset.py .unnumbered}

> import os import cv2
>
> import numpy as np import pandas as pd
>
> from torch.utils.data import Dataset from torchvision import transforms
>
> \# 6 类缺陷名称常量：训练、推理、展示共用，避免硬编码不一致 DEFECT_NAMES = \[\'Crazing\', \'Inclusion\', \'Patches\',
>
> \'Pitted Surface\', \'Rolled-in Scale\', \'Scratches\'\]
>
> DEFECT_NAMES_CN = \[\'裂纹\', \'夹杂\', \'斑块\', \'麻点\', \'氧化铁皮\', \'划痕\'\]
>
> def read_image_safe(img_path): \"\"\"安全读取图片，支持中文路径\"\"\"
>
> \# OpenCV 的 imread 不支持中文路径，改用 fromfile + imdecode img = cv2.imdecode(
>
> np.fromfile(img_path, dtype=np.uint8), cv2.IMREAD_GRAYSCALE
>
> )
>
> return img
>
> class DefectDataset(Dataset):
>
> def init (self, csv_path, img_root=None, transform=None):
>
> \# 读取 CSV 索引文件
>
> self.df = pd.read_csv(csv_path, encoding=\'utf-8-sig\') self.img_root = img_root
>
> self.transform = transform
>
> def len (self):
>
> \# 返回数据集总样本数，DataLoader 据此决定迭代次数 return len(self.df)
>
> def getitem (self, idx):
>
> \# 根据索引取一行数据 row = self.df.iloc\[idx\]
>
> img_path = os.path.join(self.img_root, row\[\'image_path\'\]) \# 读取灰度图
>
> img = read_image_safe(img_path)
>
> \# 转为 PIL 图像以便应用 torchvision 变换 img = transforms.ToPILImage()(img)
>
> if self.transform:
>
> img = self.transform(img)
>
> \# 只取第一个通道（灰度图转 RGB 后三通道相同，取其一即可） img = img\[0:1, :, :\]
>
> label = int(row\[\'label\'\]) return img, label

#### 代码逻辑解释： {#代码逻辑解释 .unnumbered}

###### DEFECT_NAMES 常量：将类别名称定义为模块级常量，训练、推理、前端展示都引用同一份，避免各处硬编码导致类别编号不一致。这是工业级代码的良好实践。

###### read_image_safe 函数：OpenCV 的 cv2.imread() 在 Windows

###### 中文路径下会读取失败（返回 None），改用 np.fromfile() 先读入内存再用 cv2.imdecode()解码，完美解决中文路径问题。这是开发中实际遇到并解决的 Bug。 {#中文路径下会读取失败返回-none改用-np.fromfile-先读入内存再用-cv2.imdecode解码完美解决中文路径问题这是开发中实际遇到并解决的-bug .unnumbered}

######  {#section-5 .unnumbered}

######  getitem 方法：PyTorch DataLoader 每次取一个 batch 时会调用此方法。返回 (img, label) 元组，img 是形状为 \[1, 200, 200\] 的张量（1 通道灰度图），label 是 0-5 的整数。

###### img\[0:1, :, :\]：灰度图转 PIL 后会变成 3 通道（RGB

###### 三通道值相同），取第一个通道保持单通道输入，与模型定义的 Conv2d(1, 32, \...) 对应。训练集和验证集使用不同的预处理变换： {#三通道值相同取第一个通道保持单通道输入与模型定义的-conv2d1-32-...-对应训练集和验证集使用不同的预处理变换 .unnumbered}

####### 代码 3-3 训练/评估预处理变换 {#代码-3-3-训练评估预处理变换 .unnumbered}

> \# 训练集：含数据增强，提升模型泛化能力 train_transform = transforms.Compose(\[
>
> transforms.Resize((200, 200)), transforms.RandomHorizontalFlip(p=0.5), \# 随机水平翻转 transforms.RandomVerticalFlip(p=0.5), \# 随机垂直翻转 transforms.RandomRotation(10), \# 随机旋转 ±10° transforms.ToTensor(), \# 转为 \[0,1\] 张量 transforms.Normalize(mean=\[0.5\], std=\[0.5\]), \# 归一化到 \[-1,1\]
>
> \])
>
> \# 验证/测试集：无增强，保证评估结果稳定 eval_transform = transforms.Compose(\[
>
> transforms.Resize((200, 200)), transforms.ToTensor(), transforms.Normalize(mean=\[0.5\], std=\[0.5\]),
>
> \])

# 第四章 系统实现 {#第四章-系统实现 .unnumbered}

## 前端 UI 层实现

###### 前端采用 HTML5 + CSS3 + JavaScript + Bootstrap 构建响应式页面，使用 Chart.js绘制统计图表。前端通过 fetch API 与后端 REST 接口通信，实现前后端分离。 {#前端采用-html5-css3-javascript-bootstrap-构建响应式页面使用-chart.js绘制统计图表前端通过-fetch-api-与后端-rest-接口通信实现前后端分离 .unnumbered}

###### 核心页面包括： {#核心页面包括 .unnumbered}

###### index.html（检测首页）：图片上传区域、检测按钮、检测结果展示区（原图、预处理图、预测类别、置信度、各类别概率条形图）；

###### history.html（历史记录页）：检测记录列表、按时间/类别筛选、分页浏览；

###### stats.html（统计分析页）：各类缺陷分布饼图、检测趋势折线图。前端调用后端 API 的核心代码（main.js 关键逻辑）：

####### 代码 4-1 前端调用检测 API 核心代码 {#代码-4-1-前端调用检测-api-核心代码 .unnumbered}

> // 图片上传并调用检测 API async function detectDefect() {
>
> const fileInput = document.getElementById(\'imageUpload\'); const formData = new FormData(); formData.append(\'image\', fileInput.files\[0\]);
>
> try {
>
> const response = await fetch(\'/api/detect\', { method: \'POST\',
>
> body: formData
>
> });
>
> const result = await response.json();
>
> // 展示预测结果 document.getElementById(\'predictedClass\').textContent =
>
> result.predicted_name_cn; document.getElementById(\'confidence\').textContent =
>
> result.confidence_percent + \'%\';
>
> // 绘制各类别概率条形图 drawProbabilityChart(result.probabilities, result.class_names_cn);
>
> } catch (error) { console.error(\'检测失败:\', error); alert(\'检测失败，请重试\');
>
> }
>
> }

#### 代码逻辑解释： {#代码逻辑解释-1 .unnumbered}

###### FormData：用于封装文件上传数据，浏览器会自动设置 multipart/form-data 编码；

###### fetch(\'/api/detect\')：向后端 /api/detect 接口发送 POST

###### 请求，这是前后端通信的核心入口。路径必须与后端 Flask 路由定义一致； {#请求这是前后端通信的核心入口路径必须与后端-flask-路由定义一致 .unnumbered}

###### result.json()：解析后端返回的 JSON 数据，包含预测类别、置信度、各类别概率；

###### 异步处理：使用 async/await 语法，避免回调地狱，检测过程中页面不卡死。

## 后端服务层实现

###### 后端基于 Python Flask 框架，提供 RESTful API {#后端基于-python-flask-框架提供-restful-api .unnumbered}

###### 接口，负责接收前端请求、调度算法模块、操作数据库、返回 JSON响应。后端是连接前端、算法、数据库的核心枢纽。 {#接口负责接收前端请求调度算法模块操作数据库返回-json响应后端是连接前端算法数据库的核心枢纽 .unnumbered}

###### 后端核心路由设计： {#后端核心路由设计 .unnumbered}

  -------------------------- -------- --------------------------------------------------
  路由                       方法     功能说明

  /                          GET      返回检测首页

  /api/detect                POST     接收图片，调用模型推理，返回检测结果并写入数据库

  /api/history               GET      查询历史检测记录，支持分页和筛选

  /api/stats                 GET      返回统计数据（各类缺陷数量、检测趋势）

  /api/upload/\<filename\>   GET      返回上传的图片文件
  -------------------------- -------- --------------------------------------------------

####### 表 4-1 后端 API 路由设计 {#表-4-1-后端-api-路由设计 .unnumbered}

###### 后端检测接口核心代码（app.py 关键逻辑）： {#后端检测接口核心代码app.py-关键逻辑 .unnumbered}

####### 代码 4-2 后端检测接口核心代码（app.py） {#代码-4-2-后端检测接口核心代码app.py .unnumbered}

> import os
>
> from flask import Flask, request, jsonify, send_from_directory from werkzeug.utils import secure_filename
>
> import sqlite3
>
> from datetime import datetime import sys
>
> sys.path.insert(0, os.path.join(os.path.dirname( file ), \'..\', \'src\'))
>
> from inference import DefectPredictor
>
> app = Flask( name , static_folder=\'../web/static\', template_folder=\'../web/templates\')
>
> app.config\[\'MAX_CONTENT_LENGTH\'\] = 10 \* 1024 \* 1024 \# 限制上传 10MB
>
> \# 全局加载模型（只加载一次，避免每次请求重复加载） predictor = DefectPredictor(model_path=\'models/best_model.pth\')
>
> \@app.route(\'/api/detect\', methods=\[\'POST\'\]) def detect():
>
> \# 1. 校验文件
>
> if \'image\' not in request.files:
>
> return jsonify({\'error\': \'未上传图片\'}), 400 file = request.files\[\'image\'\]
>
> if file.filename == \'\':
>
> return jsonify({\'error\': \'文件名为空\'}), 400
>
> \# 2. 保存上传图片
>
> filename = secure_filename(file.filename) upload_dir = \'web/static/uploads\' os.makedirs(upload_dir, exist_ok=True) save_path = os.path.join(upload_dir, filename) file.save(save_path)
>
> \# 3. 调用模型推理
>
> result = predictor.predict(save_path)
>
> \# 4. 写入数据库
>
> conn = sqlite3.connect(\'defect_detection.db\') conn.execute(\"\"\"
>
> INSERT INTO detection_records
>
> (image_path, predicted_class, predicted_name, confidence, detect_time) VALUES (?, ?, ?, ?, ?)
>
> \"\"\", (filename, result\[\'predicted_class\'\], result\[\'predicted_name_cn\'\], result\[\'confidence\'\], datetime.now().strftime(\'%Y-%m-%d %H:%M:%S\')))
>
> conn.commit() conn.close()
>
> \# 5. 返回 JSON 结果 return jsonify(result)

#### 代码逻辑解释： {#代码逻辑解释-2 .unnumbered}

####  {#section-6 .unnumbered}

###### （1）MAX_CONTENT_LENGTH = {#max_content_length .unnumbered}

###### 10MB：限制上传文件大小，防止大图片导致内存溢出。这是开发中 AI初版代码遗漏、人工补充的安全校验。 {#mb限制上传文件大小防止大图片导致内存溢出这是开发中-ai初版代码遗漏人工补充的安全校验 .unnumbered}

###### predictor 全局加载：模型在 Flask 启动时加载一次，存在全局变量中。如果每次请求都加载模型，推理延迟会增加几秒，用户体验极差。这是后端性能优化的关键。

###### secure_filename()：过滤文件名中的特殊字符（如

###### ../、路径分隔符），防止路径穿越攻击（用户上传名为 ../../etc/passwd 的文件）。这是 Web安全的基本要求。 {#路径分隔符防止路径穿越攻击用户上传名为-....etcpasswd-的文件这是-web安全的基本要求 .unnumbered}

###### 五步处理流程：校验文件 → 保存图片 → 模型推理 → 写入数据库 →返回结果，构成完整的业务闭环。每一步都有明确的错误处理。

###### sqlite3 直接操作：使用 Python 内置的 sqlite3

###### 模块，无需额外安装数据库服务，适合课程设计的轻量级场景。 {#模块无需额外安装数据库服务适合课程设计的轻量级场景 .unnumbered}

## 数据存储层实现

###### 数据库采用 SQLite，是 Python {#数据库采用-sqlite是-python .unnumbered}

###### 内置的轻量级嵌入式数据库，无需额外安装和配置，数据存储在单个 .db文件中，便于项目打包和迁移。 {#内置的轻量级嵌入式数据库无需额外安装和配置数据存储在单个-.db文件中便于项目打包和迁移 .unnumbered}

###### 数据库表设计（detection_records 检测记录表）： {#数据库表设计detection_records-检测记录表 .unnumbered}

+-----------------+--------------+-------------------------+-----------------------------------+
| 字段名          | > 类型       | > 约束                  | 说明                              |
+-----------------+--------------+-------------------------+-----------------------------------+
| id              | > INTEGERPRI | > MARY KEY AUTOINCREMEN | > T 记录唯一标识，自增主键        |
+-----------------+--------------+-------------------------+-----------------------------------+
| image_path      | > TEXT       | > NOT NULL              | > 上传图片的保存路径              |
+-----------------+--------------+-------------------------+-----------------------------------+
| predicted_class | > INTEGER    | > NOT NULL              | > 预测类别编号（0-5）             |
+-----------------+--------------+-------------------------+-----------------------------------+
| predicted_name  | > TEXT       | > NOT NULL              | > 预测类别中文名称                |
+-----------------+--------------+-------------------------+-----------------------------------+
| confidence      | > REAL       | > NOT NULL              | > 预测置信度（0-1）               |
+-----------------+--------------+-------------------------+-----------------------------------+
| detect_time     | > TEXT       | > NOT NULL              | > 检测时间（YYYY-MM-DD HH:MM:SS） |
+-----------------+--------------+-------------------------+-----------------------------------+

####### 表 4-2 detection_records 表结构 {#表-4-2-detection_records-表结构 .unnumbered}

###### 数据库初始化代码（init_db.py）： {#数据库初始化代码init_db.py .unnumbered}

######  {#section-7 .unnumbered}

####### 代码 4-3 数据库初始化（init_db.py） {#代码-4-3-数据库初始化init_db.py .unnumbered}

> import sqlite3 import os
>
> def init_database():
>
> db_path = \'defect_detection.db\' conn = sqlite3.connect(db_path) cursor = conn.cursor()
>
> \# 创建检测记录表（IF NOT EXISTS 避免重复创建报错） cursor.execute(\"\"\"
>
> CREATE TABLE IF NOT EXISTS detection_records ( id INTEGER PRIMARY KEY AUTOINCREMENT,
>
> image_path TEXT NOT NULL, predicted_class INTEGER NOT NULL, predicted_name TEXT NOT NULL, confidence REAL NOT NULL, detect_time TEXT NOT NULL
>
> ) \"\"\")
>
> conn.commit() conn.close()
>
> print(f\'数据库初始化完成: {db_path}\')
>
> if name == \' main \':
>
> init_database()

###### 历史记录查询接口核心代码： {#历史记录查询接口核心代码 .unnumbered}

####### 代码 4-4 历史记录查询接口 {#代码-4-4-历史记录查询接口 .unnumbered}

> \@app.route(\'/api/history\', methods=\[\'GET\'\]) def get_history():
>
> \# 从 URL 参数获取筛选条件
>
> page = int(request.args.get(\'page\', 1))
>
> page_size = int(request.args.get(\'page_size\', 10)) defect_class = request.args.get(\'class\', \'\')
>
> conn = sqlite3.connect(\'defect_detection.db\')
>
> conn.row_factory = sqlite3.Row \# 让查询结果可以用列名访问
>
> \# 动态构建 SQL：有类别筛选时加 WHERE 条件 query = \'SELECT \* FROM detection_records\' params = \[\]
>
> if defect_class:
>
> query += \' WHERE predicted_name = ?\' params.append(defect_class)
>
> query += \' ORDER BY detect_time DESC LIMIT ? OFFSET ?\' params.extend(\[page_size, (page - 1) \* page_size\])
>
> rows = conn.execute(query, params).fetchall() conn.close()
>
> \# 将 Row 对象转为字典列表，方便 jsonify 序列化 records = \[dict(row) for row in rows\]
>
> return jsonify({\'records\': records, \'page\': page})

## 算法模块层------CNN 模型设计

###### 算法模块是系统的核心，负责从图像中自动提取缺陷特征并分类。本项目采用轻量化 CNN 网络，在保证分类准确率的同时控制参数量，适合 CPU 环境部署。 {#算法模块是系统的核心负责从图像中自动提取缺陷特征并分类本项目采用轻量化-cnn-网络在保证分类准确率的同时控制参数量适合-cpu-环境部署 .unnumbered}

###### 模型整体结构： {#模型整体结构 .unnumbered}

+------------------------+------------------+---------+-----------------------------------------+
| > 层名称               | > 输出形状       | 参数量  | 说明                                    |
+------------------------+------------------+---------+-----------------------------------------+
| > 输入                 | \[1, 200, 200\]  | ---     | 单通道 200×200 灰度图                   |
+------------------------+------------------+---------+-----------------------------------------+
| > Conv Block 1         | \[32, 100, 100\] | 320     | Conv(1→32, 3×3) + BN + ReLU + MaxPool   |
+------------------------+------------------+---------+-----------------------------------------+
| > Conv Block 2         | \[64, 50, 50\]   | 18,496  | Conv(32→64, 3×3) + BN + ReLU + MaxPool  |
+------------------------+------------------+---------+-----------------------------------------+
| > Conv Block 3         | \[128, 25, 25\]  | 73,856  | Conv(64→128, 3×3) + BN + ReLU + MaxPool |
+------------------------+------------------+---------+-----------------------------------------+
| > AdaptiveAvgPool      | > \[128, 1, 1\]  | 0       | 自适应平均池化到 1×1                    |
+------------------------+------------------+---------+-----------------------------------------+
| > FC1 + ReLU + Dropout | > \[64\]         | 8,256   | 全连接 128→64，Dropout=0.5              |
+------------------------+------------------+---------+-----------------------------------------+
| > FC2（输出层）        | > \[6\]          | 390     | 全连接 64→6，对应 6 类缺陷              |
+------------------------+------------------+---------+-----------------------------------------+
| > 合计                 | > ---            | 101,766 | 约 10.2 万参数                          |
+------------------------+------------------+---------+-----------------------------------------+

####### 表 4-3 CNN 模型结构与参数量 {#表-4-3-cnn-模型结构与参数量 .unnumbered}

###### 模型定义完整代码（model.py）： {#模型定义完整代码model.py .unnumbered}

####### 代码 4-5 轻量化 CNN 模型定义（model.py） {#代码-4-5-轻量化-cnn-模型定义model.py .unnumbered}

> import torch
>
> import torch.nn as nn
>
> class LightweightCNN(nn.Module):
>
> \"\"\"3 个卷积块 + 2 个全连接层的轻量化 CNN\"\"\" def init (self, num_classes=6):
>
> super(LightweightCNN, self). init ()
>
> \# 卷积块 1：1 通道输入 → 32 通道输出 self.conv_block1 = nn.Sequential(
>
> nn.Conv2d(1, 32, kernel_size=3, padding=1), nn.BatchNorm2d(32), \# 批归一化，加速收敛、稳定训练 nn.ReLU(inplace=True), \# 激活函数，引入非线性 nn.MaxPool2d(2, stride=2) \# 最大池化，尺寸减半 200→100
>
> )
>
> \# 卷积块 2：32 → 64 通道 self.conv_block2 = nn.Sequential(
>
> nn.Conv2d(32, 64, kernel_size=3, padding=1), nn.BatchNorm2d(64), nn.ReLU(inplace=True),
>
> nn.MaxPool2d(2, stride=2) \# 100→50
>
> )
>
> \# 卷积块 3：64 → 128 通道 self.conv_block3 = nn.Sequential(
>
> nn.Conv2d(64, 128, kernel_size=3, padding=1), nn.BatchNorm2d(128), nn.ReLU(inplace=True),
>
> nn.MaxPool2d(2, stride=2) \# 50→25
>
> )
>
> \# 自适应平均池化：任意尺寸 → 1×1，避免全连接层尺寸写死 self.avgpool = nn.AdaptiveAvgPool2d((1, 1))
>
> \# 全连接分类器 self.classifier = nn.Sequential(
>
> nn.Linear(128, 64), nn.ReLU(inplace=True),
>
> nn.Dropout(p=0.5), \# 随机失活 50% 神经元，防止过拟合 nn.Linear(64, num_classes)
>
> )
>
> def forward(self, x): \"\"\"前向传播：定义数据从输入到输出的流动路径\"\"\" x = self.conv_block1(x)
>
> x = self.conv_block2(x) x = self.conv_block3(x) x = self.avgpool(x)
>
> x = torch.flatten(x, 1) \# 展平为 \[batch, 128\] x = self.classifier(x)
>
> return x \# 输出 \[batch, 6\]，后续用 softmax 转概率

#### 模型核心组件解释： {#模型核心组件解释 .unnumbered}

###### 卷积层 Conv2d：CNN 的核心，用 3×3

###### 的滑动窗口（卷积核）在图像上滑动，提取局部特征。padding=1保证卷积前后尺寸不变。通道数从 1→32→64→128 {#的滑动窗口卷积核在图像上滑动提取局部特征padding1保证卷积前后尺寸不变通道数从-13264128 .unnumbered}

###### 递增，低层提取边缘、纹理等简单特征，高层提取缺陷形状等复杂特征。 {#递增低层提取边缘纹理等简单特征高层提取缺陷形状等复杂特征 .unnumbered}

######  {#section-8 .unnumbered}

###### 批归一化 BatchNorm2d：对每个 batch

###### 的特征做归一化，使数据分布稳定，加速训练收敛，同时有一定正则化效果。 {#的特征做归一化使数据分布稳定加速训练收敛同时有一定正则化效果 .unnumbered}

###### ReLU 激活函数：max(0, x)，将负值置零，引入非线性。没有激活函数的话，多层卷积等价于一层线性变换，无法学习复杂模式。

###### 最大池化 MaxPool2d：取 2×2 窗口中的最大值，尺寸减半。作用：降低特征图分辨率

###### 、减少计算量、扩大感受野、增强平移不变性。 {#减少计算量扩大感受野增强平移不变性 .unnumbered}

###### 自适应平均池化 AdaptiveAvgPool2d：不管输入尺寸多大，都池化到 1×1。这样全连接层的输入维度固定为 128，模型可以处理不同尺寸的输入图片。

###### Dropout(p=0.5)：训练时随机让 50% 的神经元失活，迫使模型不依赖某些特定神经元，防止过拟合。推理时自动关闭（model.eval()）。

###### forward 方法：定义前向传播路径。PyTorch 自动根据 forward

###### 计算梯度，不需要手动写反向传播。输出是 6 维向量（未归一化的 logits），用 softmax转为概率。 {#计算梯度不需要手动写反向传播输出是-6-维向量未归一化的-logits用-softmax转为概率 .unnumbered}

## 算法模块层------模型训练

###### 模型训练脚本（train.py）实现了完整的训练循环，包括数据加载、前向传播、损失计算 {#模型训练脚本train.py实现了完整的训练循环包括数据加载前向传播损失计算 .unnumbered}

###### 、反向传播、参数更新、验证评估、最佳模型保存、训练曲线绘制。训练超参数配置： {#反向传播参数更新验证评估最佳模型保存训练曲线绘制训练超参数配置 .unnumbered}

+------------------------+----------------------------+-------------------------------------+
| > 超参数               | > 取值                     | > 说明                              |
+------------------------+----------------------------+-------------------------------------+
| > 训练轮数 Epochs      | > 30（实际 10 轮已收敛）   | > 完整遍历训练集的次数              |
+------------------------+----------------------------+-------------------------------------+
| > 批次大小 Batch Size  | > 32                       | > 每次迭代喂入模型的样本数          |
+------------------------+----------------------------+-------------------------------------+
| > 学习率 Learning Rate | > 0.001                    | > 参数更新步长，Adam 默认值         |
+------------------------+----------------------------+-------------------------------------+
| > 优化器               | > Adam                     | > 自适应学习率优化器，收敛快        |
+------------------------+----------------------------+-------------------------------------+
| > 损失函数             | > CrossEntropyLoss         | > 多分类交叉熵损失                  |
+------------------------+----------------------------+-------------------------------------+
| > 学习率调度 St        | > epLR(step=10, gamma=0.5) | > 每 10 轮学习率减半                |
+------------------------+----------------------------+-------------------------------------+
| > 权重衰减             | > 1e-4                     | > L2 正则化，防止过拟合             |
+------------------------+----------------------------+-------------------------------------+

+--------------+----------------+-------------------------------------+
| > 随机种子   | > 42           | > 保证实验可复现                    |
+--------------+----------------+-------------------------------------+

####### 表 4-4 训练超参数 {#表-4-4-训练超参数 .unnumbered}

###### 训练循环核心代码： {#训练循环核心代码 .unnumbered}

####### 代码 4-6 训练循环与最佳模型保存 {#代码-4-6-训练循环与最佳模型保存 .unnumbered}

> def train_one_epoch(model, loader, criterion, optimizer, device):
>
> model.train() \# 设置为训练模式（Dropout、BatchNorm 生效） running_loss = 0.0
>
> correct = 0
>
> total = 0
>
> for images, labels in loader:
>
> images, labels = images.to(device), labels.to(device) optimizer.zero_grad() \# 清零上一轮梯度 outputs = model(images) \# 前向传播
>
> loss = criterion(outputs, labels) \# 计算损失 loss.backward() \# 反向传播，计算梯度 optimizer.step() \# 更新参数
>
> \# 统计准确率
>
> running_loss += loss.item() \* images.size(0)
>
> \_, predicted = outputs.max(1) total += labels.size(0)
>
> correct += predicted.eq(labels).sum().item() return running_loss / total, 100.0 \* correct / total
>
> \# 最佳模型保存逻辑
>
> if val_acc \> best_val_acc:
>
> best_val_acc = val_acc torch.save({
>
> \'epoch\': epoch,
>
> \'model_state_dict\': model.state_dict(), \'optimizer_state_dict\': optimizer.state_dict(), \'val_acc\': val_acc,
>
> }, \'models/best_model.pth\')

#### 训练流程解释： {#训练流程解释 .unnumbered}

###### model.train()：设置训练模式，Dropout 和 BatchNorm 生效。验证时必须用 model.eval()关闭 Dropout，否则验证结果会波动。

###### optimizer.zero_grad()：每轮迭代前清零梯度。PyTorch

###### 默认梯度累加，不清零的话梯度会越积越大，导致训练异常。 {#默认梯度累加不清零的话梯度会越积越大导致训练异常 .unnumbered}

###### loss.backward()：反向传播，自动计算每个参数的梯度。这是深度学习框架的核心优势

###### ，不需要手动推导梯度公式。 {#不需要手动推导梯度公式 .unnumbered}

######  {#section-9 .unnumbered}

###### optimizer.step()：根据梯度更新参数。Adam 优化器会自适应调整每个参数的学习率。

###### 最佳模型保存：只保存验证准确率最高的模型权重，而不是最后一轮的模型。因为最后一轮可能过拟合，验证准确率最高的模型泛化能力最好。

###### 实际训练结果（来自 history.json）： {#实际训练结果来自-history.json .unnumbered}

  ---------------------- ------------------------------------------------
  指标                   数值

  模型总参数量           101,766（约 10.2 万）

  训练轮数               10 轮

  最佳验证准确率         100.00%（第 4 轮）

  最终训练准确率         89.60%

  最终验证准确率         100.00%

  最佳模型文件           models/best_model.pth
  ---------------------- ------------------------------------------------

####### 表 4-5 训练结果汇总 {#表-4-5-训练结果汇总 .unnumbered}

###### 训练准确率曲线： {#训练准确率曲线 .unnumbered}

![](media/image1.png){width="4.896157042869642in" height="2.8681244531933507in"}

####### 图 4-1 训练与验证准确率曲线 {#图-4-1-训练与验证准确率曲线 .unnumbered}

###### 训练损失曲线： {#训练损失曲线 .unnumbered}

######  {#section-10 .unnumbered}

> ![](media/image2.png){width="4.891924759405074in" height="2.8681244531933507in"}

####### 图 4-2 训练与验证损失曲线 {#图-4-2-训练与验证损失曲线 .unnumbered}

## 算法模块层------推理封装

###### 推理封装模块（inference.py）将模型加载、图像预处理、推理、结果后处理封装为 DefectPredictor 类，供 Flask 后端调用。这是算法模块与后端服务之间的接口层。 {#推理封装模块inference.py将模型加载图像预处理推理结果后处理封装为-defectpredictor-类供-flask-后端调用这是算法模块与后端服务之间的接口层 .unnumbered}

####### 代码 4-7 推理封装类（inference.py） {#代码-4-7-推理封装类inference.py .unnumbered}

> class DefectPredictor:
>
> def init (self, model_path, device=None):
>
> self.device = device or torch.device(
>
> \'cuda\' if torch.cuda.is_available() else \'cpu\') \# 创建模型并加载训练好的权重
>
> self.model = LightweightCNN(num_classes=6).to(self.device) checkpoint = torch.load(model_path, map_location=self.device,
>
> weights_only=False) self.model.load_state_dict(checkpoint\[\'model_state_dict\'\]) self.model.eval() \# 评估模式：关闭 Dropout
>
> \# 预处理变换（与训练时 eval_transform 一致） self.transform = transforms.Compose(\[
>
> transforms.ToPILImage(), transforms.Resize((200, 200)), transforms.ToTensor(), transforms.Normalize(mean=\[0.5\], std=\[0.5\]),
>
> \])
>
> def predict(self, image_path):
>
> \# 安全读取图片（支持中文路径）
>
> img = cv2.imdecode(np.fromfile(image_path, dtype=np.uint8), cv2.IMREAD_GRAYSCALE)
>
> \# 预处理：与训练时保持一致 img_tensor = self.transform(img) img_tensor = img_tensor\[0:1, :, :\] \# 单通道
>
> img_tensor = img_tensor.unsqueeze(0).to(self.device) \# 加 batch 维度 \# 推理（不计算梯度，节省内存、加速）
>
> with torch.no_grad():
>
> outputs = self.model(img_tensor)
>
> probabilities = torch.softmax(outputs, dim=1)\[0\].cpu().numpy() \# 取概率最大的类别
>
> predicted_class = int(np.argmax(probabilities)) confidence = float(probabilities\[predicted_class\]) return {
>
> \'predicted_class\': predicted_class,
>
> \'predicted_name\': DEFECT_NAMES\[predicted_class\], \'predicted_name_cn\': DEFECT_NAMES_CN\[predicted_class\], \'confidence\': confidence,
>
> \'confidence_percent\': round(confidence \* 100, 2), \'probabilities\': \[round(float(p), 4) for p in probabilities\],
>
> }

#### 推理流程解释： {#推理流程解释 .unnumbered}

###### 模型加载：先创建模型结构（LightweightCNN），再用 load_state_dict

###### 加载训练好的权重。map_location 参数确保在没有 GPU 的机器上也能加载 GPU训练的模型。 {#加载训练好的权重map_location-参数确保在没有-gpu-的机器上也能加载-gpu训练的模型 .unnumbered}

###### 预处理一致性：推理时的预处理必须与训练时 eval_transform 完全一致（Resize→ToTen sor→Normalize），否则数据分布不匹配，模型预测会出错。这是开发中实际遇到的 Bug（AI初版遗漏 Normalize，人工修正）。

###### unsqueeze(0)：模型输入要求 \[batch, channel, H, W\] 四维张量，单张图片是 \[1, 200, 200\]

###### 三维，需要加一个 batch 维度变成 \[1, 1, 200, 200\]。 {#三维需要加一个-batch-维度变成-1-1-200-200 .unnumbered}

###### torch.no_grad()：推理时不需要计算梯度，用此上下文管理器禁用梯度计算，节省内存并加速推理。

###### softmax：模型输出是 6 维 logits（未归一化），用 softmax 转为 0-1 之间的概率，且 6个概率之和为 1。取概率最大的类别作为预测结果。

######  {#section-11 .unnumbered}

# 第五章 测试与集成调试 {#第五章-测试与集成调试 .unnumbered}

## 模型评估结果

###### 模型评估使用独立的测试集数据，计算总体准确率、各类别精确率/召回率/F1-Score，并生成混淆矩阵热力图。评估脚本为 evaluate.py。 {#模型评估使用独立的测试集数据计算总体准确率各类别精确率召回率f1-score并生成混淆矩阵热力图评估脚本为-evaluate.py .unnumbered}

###### 测试集评估结果（来自 evaluation_report.txt）： {#测试集评估结果来自-evaluation_report.txt .unnumbered}

+----------------------+------------------------------------------------+
| 指标                 | 数值                                           |
+----------------------+------------------------------------------------+
| 测试集样本数         | 27 张                                          |
+----------------------+------------------------------------------------+
| 总体准确率           | 100.00%                                        |
+----------------------+------------------------------------------------+
| 单张图片平均推理时间 | > 11.95 ms（CPU）                              |
+----------------------+------------------------------------------------+
| 宏平均精确率         | 100.00%                                        |
+----------------------+------------------------------------------------+
| 宏平均召回率         | 100.00%                                        |
+----------------------+------------------------------------------------+
| 宏平均 F1-Score      | 1.0000                                         |
+----------------------+------------------------------------------------+

####### 表 5-1 测试集评估结果 {#表-5-1-测试集评估结果 .unnumbered}

###### 各类别详细指标： {#各类别详细指标 .unnumbered}

  ------------- ------------- ------------- ------------- -----------------
  类别          精确率        召回率        F1-Score      样本数

  裂纹          100.00%       100.00%       1.0000        4

  夹杂          100.00%       100.00%       1.0000        4

  斑块          100.00%       100.00%       1.0000        4

  麻点          100.00%       100.00%       1.0000        6

  氧化铁皮      100.00%       100.00%       1.0000        4

  划痕          100.00%       100.00%       1.0000        5
  ------------- ------------- ------------- ------------- -----------------

####### 表 5-2 各类别评估指标 {#表-5-2-各类别评估指标 .unnumbered}

###### 混淆矩阵热力图： {#混淆矩阵热力图 .unnumbered}

######  {#section-12 .unnumbered}

> ![](media/image3.jpeg){width="4.135725065616798in" height="2.7164577865266843in"}

####### 图 5-1 测试集混淆矩阵热力图 {#图-5-1-测试集混淆矩阵热力图 .unnumbered}

## 系统集成测试

###### 系统集成测试覆盖从图片上传到历史查询的完整业务流程，确保各模块协同工作正常。测试脚本使用 pytest 框架，包括 API 接口测试和端到端测试。 {#系统集成测试覆盖从图片上传到历史查询的完整业务流程确保各模块协同工作正常测试脚本使用-pytest-框架包括-api-接口测试和端到端测试 .unnumbered}

###### 测试用例设计： {#测试用例设计 .unnumbered}

+------------+------------+---------------------------------+---------------------------------------+
| > 测试编号 | > 测试模块 | > 测试内容                      | > 预期结果                            |
+------------+------------+---------------------------------+---------------------------------------+
| > T01      | > 模型推理 | > 输入有效图片，调用 predict()  | > 返回 6 类概率，和为 1，置信度在 0-1 |
+------------+------------+---------------------------------+---------------------------------------+
| > T02      | > 模型推理 | > 输入不存在的图片路径          | > 抛出 FileNotFoundError 异常         |
+------------+------------+---------------------------------+---------------------------------------+
| > T03      | > API 接口 | > POST /api/detect 上传有效图片 | > 返回 200，JSON 含预测类别和置信度   |
+------------+------------+---------------------------------+---------------------------------------+
| > T04      | > API 接口 | > POST /api/detect 不上传文件   | > 返回 400 错误提示                   |
+------------+------------+---------------------------------+---------------------------------------+
| > T05      | > API 接口 | > POST /api/detect 上传超大文件 | > 返回 413 或文件大小限制提示         |
+------------+------------+---------------------------------+---------------------------------------+
| > T06      | > 数据库   | > 检测后查询历史记录            | > 新记录已写入，字段完整              |
+------------+------------+---------------------------------+---------------------------------------+
| > T07      | > API 接口 | > GET /api/history 分页查询     | > 返回指定页数的记录列表              |
+------------+------------+---------------------------------+---------------------------------------+
| > T08      | > 端到端   | > 上传→检测→存库→查询全流程     | > 全流程无报错，数据一致              |
+------------+------------+---------------------------------+---------------------------------------+

####### 表 5-3 集成测试用例 {#表-5-3-集成测试用例 .unnumbered}

###### API 测试核心代码（test_api.py）： {#api-测试核心代码test_api.py .unnumbered}

####### 代码 5-1 API 接口测试代码 {#代码-5-1-api-接口测试代码 .unnumbered}

> import pytest import io
>
> from app import app
>
> \@pytest.fixture def client():
>
> \"\"\"创建 Flask 测试客户端\"\"\" app.config\[\'TESTING\'\] = True with app.test_client() as client:
>
> yield client
>
> def test_detect_api_valid_image(client): \"\"\"测试有效图片上传检测\"\"\"
>
> \# 构造一张测试图片（二进制数据） with open(\'test_sample.png\', \'rb\') as f:
>
> image_data = f.read() response = client.post(
>
> \'/api/detect\',
>
> data={\'image\': (io.BytesIO(image_data), \'test.png\')}, content_type=\'multipart/form-data\'
>
> )
>
> assert response.status_code == 200 result = response.get_json()
>
> assert \'predicted_class\' in result
>
> assert 0 \<= result\[\'predicted_class\'\] \<= 5
>
> assert 0 \<= result\[\'confidence\'\] \<= 1 assert len(result\[\'probabilities\'\]) == 6
>
> def test_detect_api_no_file(client): \"\"\"测试不上传文件的情况\"\"\"
>
> response = client.post(\'/api/detect\', data={}) assert response.status_code == 400

###### 测试运行命令： {#测试运行命令 .unnumbered}

####### 代码 5-2 测试运行命令 {#代码-5-2-测试运行命令 .unnumbered}

> \# 运行全部测试 pytest tests/ -v
>
> \# 运行模型推理测试 python tests/test_model.py
>
> \# 运行 API 接口测试
>
> pytest tests/test_api.py -v
>
> \# 运行端到端系统测试 python tests/test_system.py

## 调试记录与问题解决

###### 开发过程中遇到并解决了多个典型问题，以下是调试记录，体现了 Vibe Coding 中\"AI生成 → 人工审查 → 发现错误 → 反馈修正\"的闭环过程。 {#开发过程中遇到并解决了多个典型问题以下是调试记录体现了-vibe-coding-中ai生成-人工审查-发现错误-反馈修正的闭环过程 .unnumbered}

####### AI 初版 app.p1y 未限制上传文件大小，大图片缺导少致文内件存大溢小出添校加验MAX_CONTENT_LENGTH = 10MB 限制，超出返回友好提示 {#ai-初版-app.p1y-未限制上传文件大小大图片缺导少致文内件存大溢小出添校加验max_content_length-10mb-限制超出返回友好提示 .unnumbered}

####### 前2端 fetch 调用路径与后端路由不匹A配PI 地址拼写错误 修正为正确的 /api/detect 路径，统一前后端接口约定 {#前2端-fetch-调用路径与后端路由不匹a配pi-地址拼写错误-修正为正确的-apidetect-路径统一前后端接口约定 .unnumbered}

####### 3 模型推理时张量维度不匹输配入图片未按模型在要in求fe归ren一ce化.py 中统一添加 transforms.Normalize(mean=\[0.5\], std=\[0.5\]) {#模型推理时张量维度不匹输配入图片未按模型在要in求fe归ren一ce化.py-中统一添加-transforms.normalizemean0.5-std0.5 .unnumbered}

####### 中文4路径在 Windows 下 OpenCOVp读en取C失V 默败认不支持中文路径改用 np.fromfile + cv2.imdecode 方式读取图片 {#中文4路径在-windows-下-opencovp读en取c失v-默败认不支持中文路径改用-np.fromfile-cv2.imdecode-方式读取图片 .unnumbered}

####### 加线程锁或改为串行写入，Flask 开发环境足够使用 {#加线程锁或改为串行写入flask-开发环境足够使用 .unnumbered}

####### 未设置 model.ev验al(证) 前调用 model.eval() 关闭 Dropout 和 BatchNorm 训练模式表 5-4 调试记录 {#未设置-model.ev验al证-前调用-model.eval-关闭-dropout-和-batchnorm-训练模式表-5-4-调试记录 .unnumbered}

#######  {#section-13 .unnumbered}

# 第六章 AI 使用披露 {#第六章-ai-使用披露 .unnumbered}

## 使用工具与环节

###### 本项目全程采用 Vibe Coding（AI 辅助人机协同开发）方法，如实披露 AI工具的使用情况如下： {#本项目全程采用-vibe-codingai-辅助人机协同开发方法如实披露-ai工具的使用情况如下 .unnumbered}

+--------------+----------------------------------+----------------------------------+--------------------------------------------------+
| 工具名称     | 类型                             | > 使用环节                       | 具体用途                                         |
+--------------+----------------------------------+----------------------------------+--------------------------------------------------+
| 豆包 AI 助手 | > 对话选式题编调程研助、手方案设 | 计、代码提解供释技、术文选档型撰 | 建写议、、调生试成辅代助码初稿、解释代码逻辑、梳 |
+--------------+----------------------------------+----------------------------------+--------------------------------------------------+
| 通义灵码     | > VS Code 插件代码               | 补全、注释生成实、时调代试码辅   | 补助全、自动生成函数注释、错误提示与修           |
+--------------+----------------------------------+----------------------------------+--------------------------------------------------+

####### 表 6-1 AI 工具使用情况 {#表-6-1-ai-工具使用情况 .unnumbered}

###### AI 使用环节覆盖： {#ai-使用环节覆盖 .unnumbered}

###### 选题调研：咨询工业缺陷检测的技术方案、数据集选择、模型选型建议；

###### 架构设计：讨论 B/S 架构分层、API 接口设计、数据库表结构；

###### 代码生成：生成模型定义、训练脚本、Flask 后端、前端页面的初稿；

###### 调试辅助：分析错误日志、提供修复方案、解释异常原因；

###### 文档撰写：梳理报告结构、润色文字表达、生成代码注释。

## 关键 Prompt 策略

###### 在 Vibe Coding 过程中，采用了以下关键 Prompt 策略来提升 AI 输出质量： {#在-vibe-coding-过程中采用了以下关键-prompt-策略来提升-ai-输出质量 .unnumbered}

###### 任务拆解：将复杂任务拆分为小步骤，逐步引导 AI 完成。例如先让 AI设计模型结构，确认后再让其写训练代码，而不是一次性要求\"写完整系统\"。

###### 约束说明：在 Prompt 中明确技术栈、输入输出格式、代码规范等约束。例如\"使用 PyTorch，输入 1 通道 200×200 灰度图，输出 6 类概率，代码要有中文注释\"。

###### 验收标准：明确告诉 AI 什么样的结果算合格。例如\"代码必须能直接运行，包含 if

######  name == \' main \' 自测入口\"。 {#name-main-自测入口 .unnumbered}

###### 上下文工程：让 AI 先读取已有代码再修改，保持代码风格一致。例如\"先读 model.py，然后基于这个模型结构写 train.py\"。

###### 错误反馈：遇到 Bug 时，将完整错误日志粘贴给 AI，要求其分析原因并给出修复方案，而不是笼统地说\"代码有问题\"。

###### 典型 Prompt 示例： {#典型-prompt-示例 .unnumbered}

####### 理报告结构复建议 {#理报告结构复建议 .unnumbered}

#######  {#section-14 .unnumbered}

####### 代码 6-1 典型 Prompt 示例 {#代码-6-1-典型-prompt-示例 .unnumbered}

> \# 示例 1：模型设计 Prompt
>
> \"我要做一个工业表面缺陷分类系统，6 类缺陷，输入是 200x200 灰度图。请设计一个轻量化 CNN 模型，要求：

1.  使用 PyTorch，继承 nn.Module

2.  参数量控制在 20 万以内

3.  包含 BatchNorm 和 Dropout

4.  代码要有详细中文注释

5.  包含 main 自测入口，验证前向传播\"

> \# 示例 2：调试纠错 Prompt \"运行 train.py 时报错：
>
> RuntimeError: Given groups=1, weight of size \[32, 1, 3, 3\],
>
> expected input\[1, 3, 200, 200\] to have 1 channels, but got 3 channels instead请分析原因并给出修改方案。\"
>
> \# 示例 3：规格驱动开发 Prompt \"基于以下需求规格编写 Flask 后端：

-   POST /api/detect：接收图片，返回检测结果 JSON

-   GET /api/history：分页查询历史记录

-   限制上传文件 10MB

-   检测结果写入 SQLite 数据库

-   使用 inference.py 中的 DefectPredictor 类\"

## AI 出错案例及纠正过程

###### AI 生成的代码并非完全正确，以下是典型的 AI 出错案例及人工纠正过程，体现了\"AI生成 → 人工审查 → 发现错误 → 反馈修正\"的完整闭环。 {#ai-生成的代码并非完全正确以下是典型的-ai-出错案例及人工纠正过程体现了ai生成-人工审查-发现错误-反馈修正的完整闭环 .unnumbered}

#### 案例 1：数据集划分未分层 {#案例-1数据集划分未分层 .unnumbered}

###### AI 初版问题：AI 生成的 preprocess.py 使用普通随机划分（train_test_split 不加 stratify），导致某个类别在训练集中只有几张图片，模型无法充分学习该类别特征。 {#ai-初版问题ai-生成的-preprocess.py-使用普通随机划分train_test_split-不加-stratify导致某个类别在训练集中只有几张图片模型无法充分学习该类别特征 .unnumbered}

###### 发现过程：运行预处理后打印各类别分布，发现训练集中某类别仅 5 张，测试集中有 25张，比例严重失衡。 {#发现过程运行预处理后打印各类别分布发现训练集中某类别仅-5-张测试集中有-25张比例严重失衡 .unnumbered}

###### 纠正方案：在 train_test_split 中添加 stratify=df\[\'label\'\] 参数，改为分层抽样，保证每个子集的类别分布与整体一致。修正后各类别在训练集/验证集/测试集中均为 70%/15%/15%。 {#纠正方案在-train_test_split-中添加-stratifydflabel-参数改为分层抽样保证每个子集的类别分布与整体一致修正后各类别在训练集验证集测试集中均为-701515 .unnumbered}

#### 案例 2：中文路径 OpenCV 读取失败 {#案例-2中文路径-opencv-读取失败 .unnumbered}

###### AI 初版问题：AI 生成的代码使用 cv2.imread(img_path) 读取图片，在 Windows中文路径下返回 None，导致后续处理报错。 {#ai-初版问题ai-生成的代码使用-cv2.imreadimg_path-读取图片在-windows中文路径下返回-none导致后续处理报错 .unnumbered}

######  {#section-15 .unnumbered}

###### 发现过程：在 Windows 环境运行时，图片路径包含\"缺陷检测\"中文字符，cv2.imread 返回 None，后续 transform 报错\"输入为空\"。 {#发现过程在-windows-环境运行时图片路径包含缺陷检测中文字符cv2.imread-返回-none后续-transform-报错输入为空 .unnumbered}

###### 纠正方案：改用 np.fromfile(img_path, dtype=np.uint8) 先将文件读入内存，再用 cv2.imdecode() 解码。这种方式不依赖操作系统的路径编码，完美支持中文路径。在 dataset.py 和 inference.py 中均做了此修正。 {#纠正方案改用-np.fromfileimg_path-dtypenp.uint8-先将文件读入内存再用-cv2.imdecode-解码这种方式不依赖操作系统的路径编码完美支持中文路径在-dataset.py-和-inference.py-中均做了此修正 .unnumbered}

#### 案例 3：推理时遗漏 Normalize {#案例-3推理时遗漏-normalize .unnumbered}

###### AI 初版问题：AI 生成的 inference.py 中预处理只有 Resize 和 ToTensor，遗漏了 Normalize(mean=\[0.5\], std=\[0.5\])，导致推理时数据分布与训练时不一致，模型预测结果异常 {#ai-初版问题ai-生成的-inference.py-中预处理只有-resize-和-totensor遗漏了-normalizemean0.5-std0.5导致推理时数据分布与训练时不一致模型预测结果异常 .unnumbered}

###### （置信度很低或类别错误）。 {#置信度很低或类别错误 .unnumbered}

###### 发现过程：用训练集图片测试推理，预测结果与真实标签不符，对比 dataset.py 中的 eval_transform 发现缺少 Normalize。 {#发现过程用训练集图片测试推理预测结果与真实标签不符对比-dataset.py-中的-eval_transform-发现缺少-normalize .unnumbered}

###### 纠正方案：在 inference.py 的 transform 中添加 Normalize，确保推理预处理与训练时完全一致 {#纠正方案在-inference.py-的-transform-中添加-normalize确保推理预处理与训练时完全一致 .unnumbered}

###### 。这是深度学习部署中的常见陷阱------训练和推理的预处理必须统一。 {#这是深度学习部署中的常见陷阱训练和推理的预处理必须统一 .unnumbered}

#### 案例 4：后端缺少文件大小限制 {#案例-4后端缺少文件大小限制 .unnumbered}

###### AI 初版问题：AI 生成的 app.py 没有限制上传文件大小，用户上传大图片（如 50MB）时可能导致服务器内存溢出。 {#ai-初版问题ai-生成的-app.py-没有限制上传文件大小用户上传大图片如-50mb时可能导致服务器内存溢出 .unnumbered}

###### 发现过程：代码审查时发现缺少文件大小校验，属于 Web 安全基本要求。 {#发现过程代码审查时发现缺少文件大小校验属于-web-安全基本要求 .unnumbered}

###### 纠正方案：添加 app.config\[\'MAX_CONTENT_LENGTH\'\] = 10 \* 1024 \* 1024，限制上传文件最大 10MB，超出时 Flask 自动返回 413 错误。 {#纠正方案添加-app.configmax_content_length-10-1024-1024限制上传文件最大-10mb超出时-flask-自动返回-413-错误 .unnumbered}

######  {#section-16 .unnumbered}

# 第七章 总结与展望 {#第七章-总结与展望 .unnumbered}

1.  项目总结

###### 本项目完成了一套基于轻量化卷积神经网络的工业零部件表面缺陷智能检测系统，实现了从数据准备、模型训练到系统部署的完整流程。主要完成工作包括： {#本项目完成了一套基于轻量化卷积神经网络的工业零部件表面缺陷智能检测系统实现了从数据准备模型训练到系统部署的完整流程主要完成工作包括 .unnumbered}

###### 数据资源构建：以 NEU-CLS 数据集为参考，完成数据索引生成、分层抽样划分（70%/ 15%/15%）、预处理变换设计，保证数据可复现；

###### 算法模块开发：设计并实现了 3 卷积块 + 2 全连接层的轻量化 CNN 模型（约 10.2

###### 万参数），完成模型训练、评估和推理封装，测试集准确率 100%，单张推理时间约 12ms； {#万参数完成模型训练评估和推理封装测试集准确率-100单张推理时间约-12ms .unnumbered}

###### 系统集成：采用 B/S 四层架构，实现 Flask 后端 REST API、SQLite数据库存储、前端页面交互，各模块通过标准接口协同工作；

###### 测试与调试：编写自动化测试用例覆盖模型推理、API接口、端到端流程，记录并解决了 7 个典型开发问题；

###### Vibe Coding 实践：全程使用 AI 辅助开发，形成了\"任务拆解 → 约束说明 → AI 生成 →人工审查 → 调试修正\"的有效工作模式，并如实披露 AI 使用过程。

###### 项目覆盖了《制造智能技术》课程的三个核心技术方向：工业机器视觉与图像处理技术、工业机器学习与智能分类识别技术、工业质量数据管理与追溯技术，每个方向均有实际功能支撑，非装饰性引入。 {#项目覆盖了制造智能技术课程的三个核心技术方向工业机器视觉与图像处理技术工业机器学习与智能分类识别技术工业质量数据管理与追溯技术每个方向均有实际功能支撑非装饰性引入 .unnumbered}

2.  不足与改进方向

###### 数据集规模：当前开发验证使用合成数据，真实 NEU-CLS 数据集（1800 张）上的性能有待进一步验证。后续可下载完整数据集重新训练，并进行交叉验证以获得更可靠的性能评估。

###### 模型精度：当前模型为简单的 3 层 CNN，可尝试引入 ResNet残差连接、注意力机制（SE

###### Block）、迁移学习等技术进一步提升分类准确率，尤其是对相似缺陷类别的区分能力。 {#block迁移学习等技术进一步提升分类准确率尤其是对相似缺陷类别的区分能力 .unnumbered}

###### 缺陷定位：当前系统只做缺陷分类（判断是哪类缺陷），未实现缺陷定位（在图中标出缺陷位置）。后续可引入 YOLO、Faster R-CNN

###### 等目标检测算法，实现缺陷的检测与定位一体化。 {#等目标检测算法实现缺陷的检测与定位一体化 .unnumbered}

###### 系统性能：当前为 Flask 开发服务器，并发能力有限。生产环境可改用 Gunicorn + Nginx 部署，添加 Redis 缓存，支持更高并发。

######  {#section-17 .unnumbered}

###### 用户管理：当前系统无用户认证，后续可添加用户注册登录、权限管理、操作日志等功能，满足企业级使用需求。

3.  展望

###### 随着工业 4.0 和智能制造的推进，基于机器视觉和深度学习的表面缺陷检测将在更多制造场景中落地。未来的发展方向包括：（1）少样本学习，解决工业场景中缺陷样本稀缺的问题；（2）边缘部署，将模型量化压缩后部署到工业相机边缘端，实现实时检测；（3）多模态融合，结合红外、超声波等检测手段提升检测可靠性；（4）闭环质量控制，将检测结果反馈到生产参数调整，实现质量的主动控制。本项目作为课程设计，为这些方向的深入探索奠定了基础。 {#随着工业-4.0-和智能制造的推进基于机器视觉和深度学习的表面缺陷检测将在更多制造场景中落地未来的发展方向包括1少样本学习解决工业场景中缺陷样本稀缺的问题2边缘部署将模型量化压缩后部署到工业相机边缘端实现实时检测3多模态融合结合红外超声波等检测手段提升检测可靠性4闭环质量控制将检测结果反馈到生产参数调整实现质量的主动控制本项目作为课程设计为这些方向的深入探索奠定了基础 .unnumbered}

######  {#section-18 .unnumbered}

# 参考文献 {#参考文献 .unnumbered}

###### 张智海等. 制造智能技术基础\[M\]. 北京：清华大学出版社，2022.

###### 杨杰. 人工智能基础\[M\]. 北京：机械工业出版社，2025.

###### Song K, Yan Y. A noise robust method for surface defect detection of steel products\[J\]. IEEE Transactions on Instrumentation and Measurement, 2013, 62(3): 645-658.

###### He K, Zhang X, Ren S, et al. Deep Residual Learning for Image Recognition\[C\]. IEEE Conference on Computer Vision and Pattern Recognition (CVPR), 2016: 770-778.

###### LeCun Y, Bengio Y, Hinton G. Deep learning\[J\]. Nature, 2015, 521(7553): 436-444.

###### NEU 表面缺陷数据集\[EB/OL\]. https://faculty.neu.edu.cn/songkechen/zh_CN/zhym/263269/list/index.htm

###### Flask 官方文档\[EB/OL\]. https://flask.palletsprojects.com/, 2024.

###### PyTorch 官方文档\[EB/OL\]. https://pytorch.org/docs/, 2024.

###### OpenCV 官方文档\[EB/OL\]. https://docs.opencv.org/, 2024.

###### Howard A G, Zhu M, Chen B, et al. MobileNets: Efficient Convolutional Neural Networks for Mobile Vision Applications\[J\]. arXiv preprint arXiv:1704.04861, 2017.

###### --- 报告完 --- {#报告完 .unnumbered}
