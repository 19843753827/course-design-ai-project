# 基于机器视觉的工业产品表面缺陷智能检测系统 方案设计

## 1. 功能需求分析
### 1.1 前端用户功能
- 图片上传功能：支持单张/批量上传工业工件表面图片，支持JPG、PNG等主流格式
- 检测触发功能：一键启动缺陷检测，实时展示检测进度
- 结果展示功能：标注缺陷位置与类型，展示置信度、检测时间等信息
- 历史记录功能：按时间、缺陷类型查询历史检测记录，支持查看原图与检测结果
- 检测报告功能：生成单条检测记录的简易报告，支持页面预览

### 1.2 后端服务功能
- 请求接收与响应：接收前端上传的图片与检测请求，返回结构化检测结果
- 业务逻辑调度：调度算法模块执行推理，协调数据库读写操作
- 接口管理：提供RESTful风格API，支持前后端解耦与后续功能扩展
- 文件管理：工业检测图片的本地存储与路径管理

### 1.3 算法检测功能
- 图像预处理：自动完成灰度化、降噪、尺寸归一化处理
- 缺陷识别推理：基于训练好的CNN模型输出缺陷类型与置信度
- 结果后处理：过滤低置信度结果，输出标准化检测数据

### 1.4 数据管理功能
- 检测记录存储：保存图片路径、缺陷类型、置信度、检测时间等结构化数据
- 数据查询检索：支持多条件组合查询历史检测记录
- 数据统计基础：为后续质量分析提供数据支撑

## 2. 方案论证
### 2.1 架构选型论证
| 架构类型 | 优点 | 缺点 | 选型结论 |
|---------|------|------|---------|
| B/S架构 | 无需安装客户端、部署简单、跨终端访问、适合课程Demo展示 | 实时性略低于C/S | ✅ 选用 |
| C/S架构 | 实时性强、界面表现力强 | 需要安装客户端、部署繁琐、适配性差 | ❌ 不选用 |

选型理由：本项目为课程设计Demo，侧重功能完整与快速部署，B/S架构无需额外安装，浏览器即可访问，便于演示与提交验收，完全满足工业质检场景的非高速流水线检测需求。

### 2.2 算法方案论证
| 算法方案 | 优点 | 缺点 | 选型结论 |
|---------|------|------|---------|
| 轻量化CNN模型 | 特征提取能力强、泛化性好、适配多类缺陷、工业应用成熟 | 需要一定样本量训练 | ✅ 选用 |
| 传统图像处理（边缘检测+模板匹配） | 无需训练、速度快 | 泛化性差、对复杂缺陷识别率低 | ❌ 不选用 |

选型理由：卷积神经网络是当前工业缺陷检测的主流技术方案，对应课程核心教学内容，且公开数据集样本充足，可在本地完成训练，检测效果与技术学习价值均优于传统方法。

### 2.3 技术栈可行性论证
选用Python生态技术栈，轻量化、易上手、文档资源丰富，适合课程设计快速开发；所有组件均为开源免费工具，无需商业授权，本地普通电脑即可运行，无算力门槛。

## 3. 总体方案规划
### 3.1 整体技术栈选型
| 模块 | 技术选型 | 说明 |
|------|----------|------|
| 前端UI | HTML5 + CSS3 + JavaScript + Bootstrap | 快速构建响应式工业界面 |
| 后端服务 | Python Flask | 轻量级Web框架，快速搭建REST API |
| 算法框架 | OpenCV + PyTorch | OpenCV做图像处理，PyTorch搭建CNN模型 |
| 数据库 | SQLite | 轻量化嵌入式数据库，无需额外部署 |
| 开发方式 | Vibe Coding智能辅助开发 | 提升开发效率，保证代码规范 |

### 3.2 前端UI方案
采用单页面应用设计，整体分为三个功能区域：
1. **操作区**：图片上传控件、检测按钮、检测状态提示
2. **结果展示区**：原图展示、缺陷标注叠加、检测结果明细表格
3. **历史记录区**：检测记录列表、查询筛选控件、详情查看入口
界面风格简洁务实，采用工业蓝灰色调，符合工业系统设计规范。

### 3.3 后端服务方案
采用分层设计，分为三层：
1. **接口层**：Flask路由，处理HTTP请求与响应
2. **业务逻辑层**：检测业务调度、文件管理、数据组装
3. **数据访问层**：封装数据库增删改查操作，与业务逻辑解耦
核心接口包括：图片上传接口、缺陷检测接口、历史记录查询接口。

### 3.4 算法模块方案
1. **预处理阶段**：图像缩放至统一尺寸→灰度化→高斯滤波降噪→像素值归一化
2. **模型结构**：采用轻量化CNN结构，包含3个卷积池化单元+2层全连接层，输出4类缺陷分类结果
3. **推理阶段**：加载训练好的模型权重，输入预处理后的图像，输出分类结果与置信度
4. **后处理阶段**：置信度阈值过滤，转换为前端可解析的JSON格式

### 3.5 数据库设计方案
设计核心数据表`inspection_records`，核心字段：
- 记录ID、图片存储路径、缺陷类型、置信度、检测时间、备注
支持按时间范围、缺陷类型进行索引，提升查询效率。

### 3.6 外部资源与数据集来源
**数据集选用东北大学公开NEU表面缺陷数据集（NEU-CLS）**，是工业缺陷检测领域经典公开数据集，完全符合作业要求，无需企业数据。
- 数据集内容：包含热轧钢板表面6类典型缺陷（裂纹、夹杂、斑块、麻点、氧化铁皮、划痕），每类300张，共1800张灰度图像
- 数据格式：200×200像素灰度图，标注清晰
- 获取方式：学术公开资源，可免费下载使用
- 适配性：本项目选取其中4类缺陷进行训练验证，样本量充足，适合课程设计模型训练

其他外部资源：
- PyTorch官方模型文档、OpenCV官方文档
- Bootstrap前端组件库、Flask官方教程

## 4. 技术路线
1. **环境搭建阶段**：配置Python开发环境，安装Flask、OpenCV、PyTorch、SQLite等依赖库，搭建项目基础目录结构
2. **数据处理阶段**：下载NEU数据集，完成数据清洗、划分训练集/验证集/测试集，完成数据增强
3. **模型开发阶段**：搭建轻量化CNN网络，进行模型训练、调参与验证，输出可用的模型权重文件
4. **后端开发阶段**：编写Flask后端接口，集成算法推理模块，实现数据库读写功能
5. **前端开发阶段**：编写前端页面，实现图片上传、结果展示、历史查询功能，完成前后端联调
6. **测试优化阶段**：全流程功能测试，修复Bug，优化界面与检测速度，完善文档
7. **验收交付阶段**：整理项目代码与文档，提交GitHub仓库，准备演示验收

## 5. 项目计划安排
| 阶段 | 时间周期 | 核心任务 | 交付物 |
|------|----------|--------|--------|
| 第一阶段 | 第1-2天 | 选题确认、方案设计、GitHub仓库搭建 | 选题说明.md、方案设计.md、学习笔记.md |
| 第二阶段 | 第3-4天 | 数据集下载处理、CNN模型搭建与训练 | 模型训练代码、模型权重文件 |
| 第三阶段 | 第5-6天 | 后端API开发、数据库设计、算法模块集成 | 后端服务代码、接口文档 |
| 第四阶段 | 第7-8天 | 前端页面开发、前后端联调 | 前端页面代码、完整可运行Demo |
| 第五阶段 | 第9-10天 | 系统测试优化、文档完善、代码提交 | 完整项目代码、验收文档 |

## 6. 参考文献
[1] 张贝克. 制造智能技术[M]. 化学工业出版社, 2022.
[2] 冈萨雷斯. 数字图像处理[M]. 电子工业出版社, 2021.
[3] 李沐. 动手学深度学习[M]. 人民邮电出版社, 2021.
[4] Song K, Yan Y. A noise robust method for surface defect detection of steel products[J]. IEEE Transactions on Instrumentation and Measurement, 2013.
[5] NEU表面缺陷数据集[EB/OL]. http://faculty.neu.edu.cn/songkechen/zh_CN/zlw/list.htm, 2018.
[6] Flask官方文档[EB/OL]. https://flask.palletsprojects.com/, 2024.
[7] PyTorch官方文档[EB/OL]. https://pytorch.org/docs/, 2024.
